{
  "name": "agentflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "check": "npm run check --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "cf-build-api": "npm ci && npm run check -w @agentflow/api"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
